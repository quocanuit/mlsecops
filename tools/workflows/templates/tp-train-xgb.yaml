apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: template-train-xgb
  namespace: argo-workflows
spec:
  templates:
    - name: train-xgb
      inputs:
        parameters:
          - name: ecr-registry
          - name: image-tag
      container:
        image: "{{inputs.parameters.ecr-registry}}/registry:{{inputs.parameters.image-tag}}"
        command: [sh, -c]
        args:
          - |
            set -e
            cd /workspace
            python /app/src/train_xgb.py
            echo "XGBoost training complete!"
            ls -lh artifacts/models/
        volumeMounts:
          - name: workspace
            mountPath: /workspace
        env:
          - name: PYTHONUNBUFFERED
            value: "1"
