apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: template-fetch-production-data
  namespace: argo-workflows
spec:
  templates:
    - name: fetch-production-data
      inputs:
        parameters:
          - name: ecr-registry
          - name: image-tag
          - name: dynamodb-table
          - name: s3-production-bucket
          - name: s3-artifact-bucket
          - name: workflow-name
          - name: max-items
            value: ""  # Optional: limit number of records to fetch
      container:
        image: "{{inputs.parameters.ecr-registry}}:{{inputs.parameters.image-tag}}"
        command: [sh, -c]
        args:
          - |
            set -e

            echo "Fetching production data from DynamoDB..."
            echo "Table: {{inputs.parameters.dynamodb-table}}"
            echo "Max items: {{inputs.parameters.max-items}}"
            
            cd /workspace
            python /app/src/fetch_production_data.py

            echo "Data fetched successfully!"
            ls -lh /workspace/data/raw/

            # Upload to workflow artifact bucket using workflows_artifacts_handler
            echo "Uploading to workflow artifacts..."
            python /app/src/utils/workflows_artifacts_handler.py \
              --bucket "{{inputs.parameters.s3-artifact-bucket}}" \
              --workflow-name "{{inputs.parameters.workflow-name}}" \
              upload-file \
              --local-path "/workspace/data/raw/Base.csv" \
              --s3-key "data/raw/Base.csv"

            echo "Production data ready for validation!"
        env:
          - name: PYTHONUNBUFFERED
            value: "1"
          - name: DYNAMODB_TABLE
            value: "{{inputs.parameters.dynamodb-table}}"
          - name: AWS_REGION
            value: "us-east-1"
          - name: OUTPUT_PATH
            value: "/workspace/data/raw/Base.csv"
          - name: MAX_ITEMS
            value: "{{inputs.parameters.max-items}}"
        resources:
          requests:
            memory: "512Mi"
            cpu: "300m"
          limits:
            memory: "1Gi"
            cpu: "800m"
