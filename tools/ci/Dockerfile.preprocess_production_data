FROM python:3.10-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    git \
    && rm -rf /var/lib/apt/lists/*

COPY tools/ci/requirements.preprocess_data.txt .

RUN pip install --no-cache-dir -r requirements.preprocess_data.txt

COPY src/preprocess_production_data.py ./src/
COPY src/utils/__init__.py ./src/utils/
COPY src/utils/workflows_artifacts_handler.py ./src/utils/

ENV PYTHONPATH=/app/src

CMD ["python", "src/preprocess_production_data.py"]
